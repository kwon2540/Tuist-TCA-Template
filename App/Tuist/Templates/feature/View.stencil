import AppUI
import ComposableArchitecture
import SwiftUI

// MARK: - View
@ViewAction(for: {{ name }}.self)
struct {{ name }}View: View {
    @Bindable var store: StoreOf<{{ name }}>

    init(store: StoreOf<{{ name }}>) {
        self.store = store
    }

    var body: some View {
        VStack {
            Text("Welcome to {{ name }} View")
                .font(.largeTitle)
                .padding()
        }
        .onAppear {
            send(.onAppear)
        }
    }
}
